[project]
name = "readme-analytics"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "boto3",
    "python-jose[cryptography]",
    "python-dotenv",
    "pydantic-settings",
]
